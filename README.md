# Stock Exchange (Deribit Price Tracker)

Тестовое задание на позицию **Junior Backend Developer**.

Приложение получает индексные цены криптовалют **BTC/USD** и **ETH/USD** с биржи **Deribit**, сохраняет их в PostgreSQL и предоставляет REST API для получения сохранённых данных.

---

## Функциональность

### 1. Сбор данных
- Каждую минуту запрашивается **index price**:
  - `btc_usd`
  - `eth_usd`
- Для каждой записи сохраняются:
  - тикер валюты
  - цена
  - время в формате UNIX timestamp
- Периодический сбор реализован с помощью **Celery + Celery Beat**
- Клиент Deribit реализован с использованием **aiohttp**

### 2. API (FastAPI)

Все методы используют **GET** и обязательный query-параметр `ticker`.

| Метод | Описание |
|-----|---------|
| `/prices?ticker=btc_usd` | Получение всех сохранённых цен по валюте |
| `/prices/latest?ticker=btc_usd` | Получение последней цены |
| `/prices/by-date?ticker=btc_usd&from_ts=...&to_ts=...` | Получение цен за период |

---

## 🧱 Стек технологий

- **Python 3.11**
- **FastAPI**
- **PostgreSQL**
- **SQLAlchemy**
- **Celery + Redis**
- **aiohttp**
- **Docker / Docker Compose**
- **pgAdmin**

---

## 🐳 Docker и сервисы

Приложение разворачивается с помощью `docker-compose` и состоит из следующих сервисов:

- `app` — FastAPI приложение
- `worker` — Celery worker
- `beat` — Celery Beat (периодические задачи)
- `db` — PostgreSQL
- `redis` — брокер сообщений для Celery
- `pgadmin` — web-интерфейс для просмотра и управления базой данных

> **pgAdmin добавлен намеренно** для удобного просмотра содержимого базы данных во время разработки и отладки.

---

## 🚀 Запуск проекта

### 1. Создать `.env`

```env
DATABASE_URL=postgresql://postgres:postgres@db:5432/prices
DERIBIT_BASE_URL=https://www.deribit.com/api/v2
REDIS_URL=redis://redis:6379/0
TICKERS=btc_usd,eth_usd
```

### 2. Запустите контейнеры

```
docker-compose up --build
```

---

## Доступные сервисы

API:
http://localhost:8000

Swagger:
http://localhost:8000/docs

pgAdmin:
http://localhost:5050


## Design decisions


### Вынос бизнес-логики

Бизнес-логика приложения вынесена в отдельный сервисный слой (PriceService, DeribitClient), а API-роуты используются только для приёма запросов и возврата ответов.
Такой подход позволяет разделить ответственность между слоями приложения, сделать код более читаемым и упростить тестирование бизнес-логики без привязки к FastAPI или HTTP-контексту.
Кроме того, это облегчает дальнейшее расширение приложения и повторное использование логики в фоновых задачах (Celery).

### Использование обязательного query-параметра ticker

Все API-методы принимают обязательный query-параметр ticker, который определяет валютную пару (например, btc_usd).
Это решение принято в соответствии с требованиями задания и позволяет сохранить единообразный интерфейс API.
Такой подход упрощает добавление новых торговых пар в будущем без изменения структуры эндпоинтов.

### Docker Compose для локального запуска

Приложение разворачивается с помощью Docker Compose, что позволяет запускать все необходимые сервисы (API, PostgreSQL, Redis, Celery, pgAdmin) одной командой.
Это упрощает локальную разработку, исключает проблемы с окружением и делает проект более воспроизводимым на других машинах.
Также такой подход приближен к реальным production-сценариям.

### Использование pgAdmin

В Docker Compose добавлен сервис pgAdmin в качестве вспомогательного инструмента для работы с базой данных.
Он используется исключительно для удобного просмотра данных, отладки и проверки корректности сохранения цен в PostgreSQL во время разработки.
pgAdmin не является обязательной частью приложения и не участвует в бизнес-логике.

### Использование .env для конфигурации

Все конфигурационные параметры и чувствительные данные (URL базы данных, Redis, внешние API, список тикеров и т.д.) вынесены в файл .env.
Это соответствует best practices и позволяет легко изменять настройки без правок кода.
Такой подход также упрощает разделение окружений (development, testing, production) и повышает безопасность проекта.
